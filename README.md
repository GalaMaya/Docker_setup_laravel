# Docker_setup_laravel


# Instalasi 

- buat folder src (project laravel)
- .env untuk database mysqlnya
- docker-compose up -d --build
- docker exec -it laravel-app command (untuk jalankan command php artisan)
